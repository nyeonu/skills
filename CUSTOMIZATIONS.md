# 커스텀 내역

이 스킬 세트는 [addyosmani/agent-skills](https://github.com/addyosmani/agent-skills)와 기존에 쓰던 plan-writer/plan-executor/adr-writer를 조합해 만들었다. 그대로 쓰지 않고 커스텀한 이유는 하나로 요약된다: **addyosmani 스킬들은 "한 에이전트가 순서대로 일하는" 전제로 쓰였고, 우리 워크플로우는 "사람 승인 게이트 + 고비용 계획/저비용 실행 분리 + 문서로 상태 관리"가 전제라서, 그 사이의 빈틈을 메워야 했다.**

각 항목은 "원본은 어땠나 → 뭐가 문제였나 → 그래서 어떻게 바꿨나" 순서로 기록한다.

## spec-writer — "스펙 쓰고 멈추는" 스킬

**원본**: addyosmani `spec-driven-development`. 스펙 작성 → 계획 → 작업 분해 → 구현까지 4단계를 한 스킬이 전부 진행한다.

**문제**: 우리 워크플로우에서 계획과 실행은 별도 스킬(task-breakdown, plan-executor)의 일이다. 원본을 그대로 쓰면 역할이 겹치고, 더 큰 문제는 에이전트가 스펙 승인을 기다리지 않고 계획까지 이어서 달린다는 것. 스펙이 반려되면 그 계획 작업은 전부 버려진다.

**변경**: 스펙 작성(Phase 1)만 남기고 뒷단계를 전부 잘라냈다. "docs/spec/에 저장하고 승인을 요청한 뒤 **종료한다**"를 명시. 여기에 세 가지를 새로 넣었다:

- **성공 기준마다 SC-1, SC-2… ID를 붙인다.** 뒤에서 계획이 이 기준을 빠짐없이 커버하는지 대조하고(task-breakdown), 구현이 이 기준을 실제로 충족하는지 검증할 때(spec-conformance-check) 항목 단위로 참조하기 위해서다. ID가 없으면 "대충 다 반영했겠지"가 된다.
- **문서 머리에 YAML frontmatter(`status: draft/approved`)를 붙인다.** 사람이 아니라 다음 스킬이 "이 스펙 승인됐나?"를 기계적으로 판단하는 용도.
- **확인 못 한 가정이 3개 쌓이면 스펙 쓰기를 멈추고 interview-me(인터뷰 스킬)로 전환한다.** 가정 위에 쓴 스펙은 스펙이 아니라 소설이기 때문.

## task-breakdown — 방법론은 addyosmani, 문서 포맷은 기존 체계

**원본 두 개를 합쳤다**: 기존 plan-writer(계획 문서 작성 스킬) + addyosmani `planning-and-task-breakdown`(작업 분해 방법론).

**문제 1 — plan-writer가 너무 많은 일을 했다.** 요구사항 인터뷰부터 계획 작성까지 한 스킬이었는데, 인터뷰는 이제 interview-me/spec-writer의 일이다. 그래서 인터뷰 절차를 들어내고, 대신 "승인된 스펙이 없으면 시작하지 않는다"는 입력 조건을 달았다.

**문제 2 — addyosmani의 계획 문서 포맷은 우리 실행기와 호환이 안 된다.** plan-executor는 계획 문서의 YAML frontmatter를 파싱해서 움직인다: `status: approved`인지 확인하고(승인 게이트), `depends_on`으로 병렬 실행 순서를 정하고, `tier`(standard/light)로 작업마다 고비용/저비용 모델을 배정한다. addyosmani 포맷은 체크박스 마크다운이라 이 자동화가 전부 죽는다. 그래서 **분해하는 요령(수직 슬라이싱, 작업 크기 제한, 고위험 먼저, 체크포인트)은 addyosmani에서 가져오고, 문서 포맷은 plan-writer 템플릿을 유지**했다.

**신설 — 추적성 매핑.** 스펙의 모든 SC-N이 계획의 어느 작업에서 충족되는지 표로 만들어 계획 문서에 넣는 것을 의무화했다. 커버 안 된 SC가 있으면 계획 반려. "계획이 스펙의 일부를 빼먹는" 사고를 계획 단계에서 잡는 예방 장치이며, 두 원본 어디에도 없던 규칙이다.

## tdd — 규율은 addyosmani, 파이프라인 연결과 테스트 도출은 신설

**원본**: addyosmani `test-driven-development`. RED-GREEN-REFACTOR 사이클, 버그 수정 시 재현 테스트 먼저(Prove-It), 테스트 피라미드, 좋은 테스트 원칙(상태 검증, DAMP, 목 최소화)을 담은 규율 스킬이다.

**문제 1 — 스킬만 만들면 파이프라인 코딩엔 효과가 없다.** 우리 워크플로우에서 코드는 plan-executor가 스폰하는 하위 에이전트가 쓰는데, **하위 에이전트는 스킬을 로드하지 않는다** — 아는 것은 받은 지시문뿐이다. 그래서 tdd를 규율의 단일 원본으로 두고, task-breakdown(계획의 작업 구조·추적성 매핑)과 plan-executor(하위 에이전트 지시문 템플릿)가 그 규율을 **새겨 넣는** 구조로 연결했다. 스킬이 직접 발동하는 것은 파이프라인 밖 코딩(버그 수정, 소규모 변경)이다.

**문제 2 — 원본에는 "어떤 테스트를 만들지"가 없다.** 사이클은 있는데 테스트 케이스 도출 방법이 없어서, 떠오르는 대로 만들면 모자란다. **테스트 도출 5출처**(① 스펙 SC → ② 동등 분할·경계값 → ③ 오류 분기 → ④ 상태·순서 → ⑤ 과거 버그)를 신설했다 — 각 테스트에 출처를 적을 수 있어야 하고, 출처를 다 돌았는데 안 나오는 케이스는 스펙의 구멍으로 드러난다. 층 판정 기준도 추가했다: 실제 인프라가 있어야 검증되는 동작(트랜잭션 롤백, 캐시 무효화)은 목 기반 유닛으로 대체 불가, 통합 테스트로 배정한다. (fanclub-service-api additional-info 갭 분석에서 확인된 패턴 — 유닛은 충실한데 롤백·캐시 실제 적중 검증이 비어 있었고, 둘 다 ADR·실버그 이력이 있는 자리였다.)

**문제 3 — 에이전트 TDD 고유의 실패 모드.** 구현을 아는 에이전트가 쓴 테스트는 구현의 거울이 된다(구현이 틀려도 통과). 대응: (a) 테스트 도출은 계획 단계(task-breakdown)의 산출물로 못 박고 실행자 재량에 두지 않는다, (b) 검증 신뢰도가 중요한 작업은 테스트 작성과 구현을 별도 작업으로 분리한다, (c) 테스트 skip·단언 완화로 만든 그린은 실패로 간주하고 plan-executor가 verify 시 대조한다.

**연쇄 수정**: task-breakdown(도출 단계 신설, 매핑을 SC→테스트→작업 3단으로), plan-template(테스트 케이스 열, "작성할 테스트" 항목), plan-executor(지시문에 TDD 순서, verify에서 skip·테스트 실재 확인), spec-conformance-check(SC 태그 번호가 현행 스펙과 일치하는지 확인 — 스펙 통합·재번호 시 추적성 끊김 방지), spec-writer(SC를 입력→기대 결과 형태로 쓰라는 한 줄).

**후속 추가 — 테스트 대조**: plan-executor 사용자 보고에 테스트 대조 표를 의무화했다. 계획의 추적성 매핑을 기준선으로 실제 테스트(실재·결과·skip)를 기계적으로 대조하는 표라서, 새 판단 단계가 아니라 기존 검증에 시각을 더하는 장치다 — 기준선 없는 사후 커버리지 판정(라인 %)은 "무엇이 커버돼야 하는가"를 재도출하는 fail-late라 채택하지 않았다. 함께, 사전 도출의 한계(구현 중 새로 생기는 오류 분기는 계획 시점에 없다)를 메우는 경로를 추가했다: 서브에이전트는 새 분기를 발견하면 테스트를 임의 추가하지 않고 보고만 하고, 보고의 "남은 문제 > 미커버 분기" 항목으로 취합돼 사용자 확인 후 task-breakdown에서 보충 계획으로 이어진다(승인 게이트 유지).

## spec-conformance-check — 완전 신규

**왜 만들었나**: 기존 검증에 구멍이 있었다. plan-executor는 "계획대로 됐는가"를 검증하고, 리뷰 스킬들은 "코드가 잘 짜였는가"를 본다. 그런데 **계획 자체가 스펙을 잘못 옮겼다면?** 계획 기준 검증은 전부 통과하고도 스펙과 다른 물건이 나온다. 이 "맞는 것을 만들었는가" 검증이 어디에도 없었다.

**핵심 설계 — 검증자 격리**: 검증은 새 서브에이전트가 하는데, **스펙 문서와 코드만 주고 계획 문서는 주지 않는다.** 계획을 보여주면 계획의 잘못된 해석까지 물려받아서 독립 검증이 안 되기 때문.

그 외 규칙:

- 판정에는 반드시 실행 증거(테스트·API 호출 결과)가 있어야 한다. "코드 읽어보니 있는 것 같다"는 금지.
- 하나라도 FAIL이면 품질 리뷰로 넘어가지 않고 계획 수정으로 되돌린다. 틀린 기능의 품질 리뷰는 낭비다.
- 검증자가 발견한 김에 고치는 것도 금지. 수정이 승인 게이트를 우회하게 되기 때문이다.

fresh-context 교차 검증 아이디어는 addyosmani `doubt-driven-development`에서 차용했다.

## plan-executor — 에이전트 간 채널을 영어 고정 계약으로

**계기**: 에이전트 간 통신 압축 검토 (caveman 스킬 도입 검토에서 출발). 검토 결과 caveman 의존은 기각했다 — (a) 서브에이전트는 스킬을 로드하지 않아 결국 규칙을 프롬프트에 베껴 넣어야 하고, (b) caveman식 산문 압축(관사·접속사 생략)은 모호함을 늘리는데, cavecrew가 실증한 절감의 본체는 문체가 아니라 **고정 출력 계약**(산문 금지 + 슬롯 형식)이라 의존 없이 직접 구현 가능하다.

**설계 — 채널별 압축 원칙**: 모호함 위험은 모든 채널에 있고, 채널마다 다른 것은 안전망의 유무다.

| 채널 | 안전망 | 정책 |
|---|---|---|
| 서브에이전트 → 오케스트레이터 (보고) | verify 직접 실행 | 영어 고정 슬롯 계약 (files/verify/tests/uncovered/blocked/notes) |
| 오케스트레이터 → 서브에이전트 (지시) | 재시도 비용뿐 | 보일러플레이트만 영어 고정 템플릿, **계획 원문은 한국어 그대로 인용** (번역 = 승인 안 받은 해석 단계) |
| 산출물 (스펙·PLAN·ADR·코드 주석·사용자 보고) | 없음 — 사람의 이해가 최종 검증 | 압축 금지, 한국어. 문서의 토큰 절감은 압축이 아니라 구조화+발췌(frontmatter, SC-N ID, 섹션 단위 인용)로 한다 |

영어 채택 근거: 같은 내용 기준 한국어 대비 토큰 ~30-50% 절감 + 소형(light) 모델의 영어 지시 이행률 우위 — 두 축이 같은 방향인 유일한 선택. 한자/문언문 압축은 기각 (글자 수 ≠ 토큰 수; Claude 토크나이저에서 한자는 글자당 ~1토큰이고, arXiv 2604.14210 실측에서 중국어 프롬프트는 해결률도 하락).

## plan-executor — 기존 스킬에 두 가지 연결 추가

**변경 1**: 완료 보고에 "다음은 spec-conformance-check"를 명시하게 했다. "전체 검증 통과 = 끝"이라는 착각을 막기 위해, 타협 금지 표에도 "전체 검증 통과했으니 스펙도 맞을 것"이라는 변명을 추가했다.

**변경 2**: ADR 관련 역할을 바꿨다 (아래 adr-writer 참고). 실행 전에 계획에 연결된 Proposed ADR이 있는지 확인하고, 실행 중 결정이 바뀌면 supersede를 제안하며, Accepted 확정은 스펙 적합성 검증 통과 후로 미룬다.

## adr-writer — 기록 시점을 결정 시점으로 당김

**원본 방식**: 구현이 다 끝난 뒤 ADR(아키텍처 결정 기록)을 쓴다.

**문제**: DB 선택 같은 되돌리기 비싼 결정은 실제로는 **계획 승인 시점**에 내려진다. 기록이 실행 완료 후면, (a) 실행이 중간에 엎어지면 결정 이력이 사라지고, (b) 결정이 "대안은 뭐였고 왜 기각했나" 포맷으로 검토받지 못한 채 승인된다.

**변경**: 2시점으로 분리했다.

- 계획 승인 직후 `Proposed`로 초안 작성 — 계획서의 "선택한 방식과 이유" 섹션이 초안 재료라 비용이 작다.
- 스펙 적합성 검증 통과 후 `Accepted`로 확정. 실행 중 결정이 바뀌면 기존 것을 고치지 않고 새 ADR로 대체(supersede)한다.

## interview-me — 기능 계약만 남기고 한국어로 재작성

**원본**: addyosmani `interview-me`. 영문이고, 문서 곳곳(개요·검증 체크리스트)이 addyosmani 스킬명(idea-refine, spec-driven-development, doubt-driven-development)을 가리켰다.

**문제**: (a) 존재하지 않는 스킬명이 실행 경로에 남아 있으면 에이전트가 그 스킬을 호출하려 든다. (b) 사용자에게 보여 주는 출력(가설·질문·재진술)이 영문 포맷이라, 한국어 산출물 원칙과 어긋난다.

**변경**: 영문 원문을 문장별로 번역하지 않고 기능 계약을 추려 한국어로 새로 썼다. 보존한 동작: 한 번에 질문 하나, 질문마다 추정 병기, 답을 반영한 추정 갱신, 목표·사용자·성공 기준·핵심 제약·제외 범위 확인, 명시적 확인 후 spec-writer 연결, 비대화형 환경에서는 추정하지 않고 막힌 이유 보고. 사용자 출력 형식은 `현재 이해 / 확신 정도 / 아직 모르는 점 / 질문 / 내 추정`으로 한국어화했다. 확신 수치(~95%)는 절대 정확도가 아니라 인터뷰 종료를 판단하는 보조 신호로만 쓰도록 명시했다 — 종료 기준의 본체는 "다음 질문 세 개의 반응을 예측할 수 있는가"다.

## 리뷰 3종 + 페르소나 2종 — 번역 + 테스트 리뷰 렌즈 1건 추가

한글 번역 (code-review-and-quality, security-and-hardening, performance-optimization + code-reviewer, security-auditor). 이후 code-review-and-quality에 한 가지를 더했다: 2단계(테스트 먼저 리뷰)의 "엣지 케이스를 커버하는가"가 리뷰어의 감에 맡겨져 있어서, tdd 스킬의 **테스트 도출 5출처를 리뷰 질문 표로 인라인**했다. 인라인한 이유는 be-review가 be-workflow 없이 단독 설치되는 플러그인이라 tdd 스킬 참조(강결합)를 피하기 위함이다 — 5출처 내용이 바뀌면 tdd SKILL.md와 이 표를 함께 갱신해야 한다. 이 렌즈는 plan-executor의 테스트 대조(계획 대비 기계적 대조 = 양)와 역할이 다르다: 리뷰는 테스트의 질(동작 검증인가, 구현의 거울인가)과 계획 단계 도출이 놓친 분기(리뷰 시점엔 실제 코드가 있다)를 잡는 독립 시선이다.

단 원본 리뷰 세트에 있던 `code-simplification`은 **일부러 뺐다** — 이건 검토 스킬이 아니라 코드를 고치는 스킬이라, 병렬로 돌리면 다른 리뷰어가 보고 있는 코드가 중간에 바뀐다. 코드 수정은 어떤 경우든 task-breakdown 루프(계획→승인→실행)를 통해서만 한다.

## 전체 — 사람용 산출물의 한국어 재구성

**원본 방식**: 초기 번역·포크가 영문 어순을 옮긴 한국어, 한국어 제목 뒤 영문 반복 병기, 사람이 읽는 보고서의 영문 출력 템플릿, 근거 없는 수치("10분짜리 ADR이 2시간짜리 논쟁을 대체한다")와 긍정 평가 의무화("잘된 점 최소 1개 포함")를 남겼다.

**문제**: 산출물(스펙·PLAN·검증 보고·리뷰 보고)의 최종 검증자는 사람인데, 번역체와 영문 템플릿은 읽는 비용을 올리고, 근거 없는 수치·칭찬은 문서의 신뢰를 깎는다. 반대로 frontmatter 필드, `SC-N`/`T-N`, `PASS`/`FAIL`, 에이전트 간 영어 슬롯은 기계 계약이라 문체 교정 대상이 아니다.

**변경**: 목차·식별자·상태·추적 관계는 고정하고 문장 표현과 분량만 손대는 원칙으로 재구성했다.

- 스펙 템플릿: `한눈에 보기 / 범위 / 확인한 사실과 가정(확인값·추정값·위험값·깨지는 조건) / 성공 기준 / 제약과 경계 / 미해결 사항`. 확인한 사실과 요구사항에서 온 가정을 표로 구분하고, 임의 수치는 추정값으로 표시해 사용자 확인을 받는다.
- PLAN 사람용 제목: `목표 / 실행에 필요한 현재 상태 / 선택한 방식과 이유 / 범위 / 추적성 / 작업 / 전체 검증 / 위험과 롤백`. frontmatter는 그대로다. "선택한 방식과 이유"에는 실제로 검토한 대안만 적는다 — 형식을 채우는 가짜 대안은 승인 근거 위조다.
- plan-executor 사용자 보고: `실행 결과 → 검증 → 테스트 대조 → 남은 문제 → 다음 단계`. 결과부터 쓴다. 영어 슬롯 계약(files/verify/tests/uncovered/blocked/notes)은 그대로다.
- spec-conformance-check 보고: `기준별 결과 / 경계 위반 / 실패 상세 / 남은 불확실성`. 빈 필수 항목은 일반론 대신 "없음"으로 쓴다 (모든 템플릿 공통 규칙).
- ADR 제목 한국어화(`상태 / 날짜 / 배경 / 결정 / 검토한 대안 / 결과와 비용 / 참고 자료`) — 상태 값(Proposed 등)은 영어 유지. 제목을 파싱하는 도구가 없음을 확인하고 바꿨다.
- be-review: 제목의 영문 반복 병기 제거, 영문 출력 템플릿 한국어화, 긍정 평가는 실제 확인했을 때만 포함, 공식 기준(Core Web Vitals)과 프로젝트 기본값 예시(성능 예산, 레이트 리미팅 수치)를 구분 표시. 중복 체크리스트 두 쌍은 통합하지 않고 내용을 동일하게 정렬했다 — 플러그인 설치 후 스킬 간 상대 경로의 안정성을 확인하지 못해서다. 한쪽을 고치면 반대쪽 사본도 함께 고쳐야 한다.
- using-agent-skills·README의 "병렬 실행은 읽기 전용에만"을 "계획 밖 병렬 실행은 읽기 전용에만"으로 명확화 — task-breakdown·plan-template·plan-executor가 일관되게 의도하는 `depends_on` 기반 계획 내 병렬 실행과의 문구 충돌을 해소한 것이며, 정책 변경이 아니다.

## 정확성 갱신 + 리뷰 3종 간소화 (be-workflow/be-review 0.1.1)

**계기**: 리뷰 스킬 3종의 본문이 스킬 발동마다 통째로 로드되는데(~2.2k 토큰씩), 그중 상당수가 Claude가 이미 아는 일반 개발 상식·프레임워크별 긴 코드 예시·같은 결론을 반복하는 표였다. 함께, 오래돼 틀린 기술 지침이 발견됐다. 원칙: **활성화 시 실제 판단을 바꾸는 지침만 본문에 남기고, 특정 기술·상황에서만 필요한 상세는 `references/`에서 조건부로 읽는다.** 워크플로우 계약(승인 게이트 2개, SC-N→테스트→T-N 추적성, 검증자 격리, frontmatter 상태 필드, TDD 핵심)은 변경하지 않았다.

**심각도·판정 계약 통일**: code-review-and-quality 스킬은 접두어 없음/Critical/Nit/Optional/FYI 체계였고 code-reviewer 에이전트는 Critical/Important/Suggestion이었다 — 같은 리뷰가 문서에 따라 다른 라벨을 내는 상태. `Critical`(머지 차단) / `Important`(머지 전 해결) / `Suggestion`(선택) + 판정 `APPROVE`/`REQUEST CHANGES`(Critical·Important 존재 시) 하나로 통일하고, 스킬과 에이전트가 같은 출력 템플릿을 쓰게 했다. security-auditor의 Critical/High/Medium/Low/Info는 보안 감사 고유 척도라 유지했다.

**보안 비밀번호 저장 기준 갱신**: 기존 "bcrypt/scrypt/argon2 (솔트 라운드 ≥ 12)"는 OWASP 현행 권고와 어긋난다. 2026-08 시점 OWASP Password Storage Cheat Sheet 기준으로 갱신 — 신규 시스템 Argon2id 우선(최소 19MiB/t=2/p=1), 대안 scrypt, bcrypt는 레거시 호환용(공식 최소 work factor 10 + 서버 측정으로 결정, 72바이트 제한), FIPS-140이면 PBKDF2. "rounds ≥ 12" 고정 규칙은 삭제하고, 알고리즘·파라미터는 바뀌므로 공식 출처 재확인 조건을 남겼다. `npm audit` 필수 규칙은 "저장소 생태계에 맞는 감사 도구"로 중립화하고(npm은 Node 예시), 결과는 심각도만이 아니라 도달 가능성·배포 경로로 판단하게 했다.

**성능 스킬 BE 라우팅**: 이 저장소의 목적이 BE 워크플로우인데 성능 본문의 절반이 이미지·React·번들·폰트였다. 본문은 BE 진단 순서(API·외부 호출 → DB·N+1 → CPU·메모리·GC → 풀·락 → 캐시·일관성 → p95/p99)로 재구성하고, 프런트엔드 상세는 삭제하지 않고 `references/performance-checklist.md`의 조건부 섹션("UI·웹 렌더링 성능이 대상일 때만 읽는다")으로 이동했다. "Synthetic과 RUM 둘 다 필수" 절대 규칙은 "필드 데이터 우선, 합성은 재현·회귀용, 필드 데이터 없으면 합성으로 작업하되 실사용자 개선을 단정하지 않음"으로 교체. Core Web Vitals에 75번째 백분위 필드 데이터 조건과 공식 기준/프로젝트 예산 구분을 명시하고, `scheduler.yield()`는 기능 탐지 + 폴백 필수로 바꿨다.

**리뷰 3종 본문에서 삭제한 것**: PR 줄 수 기준표·분할 전략표, 리뷰 응답 시간, 변경 설명 작성법, 멀티 모델 리뷰 일반론, 본문과 중복되는 완료 체크리스트·위험 신호·합리화 표. 코드 리뷰의 보안·성능 축은 "명백한 위험의 얕은 1차 확인 + 전문 스킬 연결"로 축소해 전문 스킬과의 중복을 제거했다. tdd 5출처 리뷰 렌즈는 유지 (별도 설치 가능한 플러그인이라 인라인 사본 유지 — 이번에 tdd ③ 문구 변경과 함께 동기화함).

**참고자료 두 사본 미통합**: security-checklist.md·performance-checklist.md는 code-review-and-quality와 전문 스킬 양쪽에 동일 사본으로 존재한다. 이번에도 공용 경로로 합치지 않았다 — 플러그인 설치 후 스킬 간 상대 경로 안정성이 검증되지 않아서다(심볼릭 링크도 같은 이유로 배제). 한쪽을 고치면 반대쪽도 동일하게 고치고 `diff -q`로 확인한다.

**TDD 절대값 → 판단 기준**: 유닛 80/통합 15/E2E 5 비율을 삭제하고 층 판정을 "계약, 실패 반경, 실제 인프라 필요 여부, 실행 비용"으로 바꿨다 — 비율은 결과이지 규칙이 아니고, BE 저장소마다 통합 비중이 크게 다르다. 도출 출처 ③의 "모든 `if`에 테스트"는 구현 결합 표현이라 "사용자 관찰 동작이 달라지는 의사결정 분기와 실패 경로"로 바꿨고, 5출처 밖 테스트 금지를 "출처를 설명할 수 있어야 한다"로 완화했다. code-review-and-quality의 인라인 5출처 표 ③도 같은 문구로 동기화했다. task-breakdown·plan-executor·plan-template은 5출처를 도출 경로로만 참조하고 비율·"모든 if"를 복제하지 않아 추가 동기화가 불필요했다.

**interview-me·spec-writer 간소화**: interview-me는 행동 계약(질문 1개씩+추정 병기, 재진술, 명시적 확인 게이트, 비대화형 금지) 전부 유지, 긴 대화 예시·중복 합리화 표·원본 출처 소개를 삭제하고 확신 수치(~30% 등)를 `낮음/중간/높음`으로 단순화했다 — 종료 기준의 본체는 수치가 아니라 "다음 질문 세 개 예측 가능 + 재진술 확인"이라서다. spec-writer는 스펙 템플릿을 `references/spec-template.md`로 분리하고 본문에는 읽기 조건만 남겼다 — 템플릿은 실제 작성 시점에만 필요하다. 동작 계약(작성 후 종료, draft→approved, SC-N)은 그대로다.

## 참고자료 필수 로드 계약 + 외부 기준 버전 추적 (be-workflow/be-review 0.1.2)

**계기**: 0.1.1에서 상세 내용을 `references/`로 옮기면서 로드 지시가 "참고하라" 권고형으로 남았다 — 에이전트가 참고자료를 읽지 않고 얕게 리뷰하면 축약 이전보다 놓침이 늘 수 있는 구조(0.1.1의 위험값). 권고형을 전부 **필수 로드 계약**으로 바꿨다. 로딩 비용은 계약을 약화하는 대신 파일 분리로 줄인다.

**필수 로드 계약**: security-and-hardening은 발동 시 `security-checklist.md`를 항상 읽고 시작한다. performance-optimization은 대상 영역별 필수(백엔드→backend, UI→frontend, 풀스택·불분명→둘 다). code-review-and-quality는 1단계에서 diff·작업 목적으로 로드 조건(보안 관련 변경 → security-checklist, DB·외부 호출·캐시·동시성 등 → performance-backend, UI 렌더링 → performance-frontend)을 판정하고 **발견 사항 분류 전에** 읽는다 — 불분명하면 읽는 쪽을 선택. 출력 `검증 확인`에 "읽은 참고자료"와 "전문 검토 연결"(확인 한계 + 후속 검토 대상 명시) 슬롯을 추가하고 code-reviewer 에이전트에도 동기화했다. spec-writer는 종료 체크리스트에 템플릿 대조 항목을 추가했다.

**performance-checklist 분리**: 필수 로드 계약과 로딩 비용을 양립시키기 위해 `performance-checklist.md`를 `performance-checklist-backend.md`와 `performance-checklist-frontend.md`로 분리했다 — 백엔드 작업이 프런트엔드 상세(이미지·React·폰트, 전체의 절반 이상)까지 강제로 읽는 것을 막기 위해서다. code-review-and-quality 쪽 사본도 같은 구조로 분리했고, 사본 동일성 규칙은 파일 단위로 그대로 적용된다 (이제 3쌍: security, perf-backend, perf-frontend).

**외부 기준 버전 추적**: OWASP·web.dev를 매 작업 조회하는 것은 낭비고, 조회 없이 방치하면 기준이 썩는다. 각 참고자료에 출처 메타데이터(공식 출처 / 확인일 / 출처 버전 / 추적하는 기준 / 상태)를 기록했다 — OWASP는 원본 파일 commit SHA(`a6eb0c6…`, 2026-06-24), web.dev는 Last updated(2024-10-31)와 LCP·INP·CLS 기준값. 운영 규칙: 월 1회 SHA·Last updated 변경 여부만 확인, 변경 없으면 확인일만 갱신, 변경 있으면 자동 수정하지 않고 `상태: needs_review`로 표시 후 사람이 검토해 스킬 본문·참고자료·에이전트 사본·중복 사본·CUSTOMIZATIONS.md를 함께 갱신. 작업 시점 재확인 조건(신규 비밀번호 저장 설계, 알고리즘·파라미터 변경, 운영 기본값 확정, CWV를 스펙·SLA·CI 기준으로 확정, needs_review 상태, 확인일 90일 초과+감지 결과 불명)은 각 스킬 본문에 있다. 단순 리뷰·기존 기준 적용 작업은 기록값을 쓴다.

**TDD 출처 표기 통일**: 0.1.1에서 tdd를 "출처 설명 가능"으로 완화하면서 plan-template의 "출처(①~⑤) 필수"와 충돌이 생겼다. "①~⑤는 기본 도출 경로, 5출처 밖 테스트는 `기타: <발견 근거>`로 기록"으로 tdd·task-breakdown·plan-template 세 문서를 통일했다.

## 직접 호출 계약과 외부 기준 신선도 보완 (be-review 0.1.3)

**직접 호출 에이전트의 필수 로드 계약**: 0.1.2에서는 code-review-and-quality 스킬에만 보안·성능 참고자료 로드 판정이 있었고, code-reviewer 에이전트에는 출력 슬롯만 있었다. 에이전트를 직접 호출하면 판정 절차를 우회할 수 있었다. code-reviewer에 같은 라우팅을 넣고, security-auditor에는 보안 체크리스트 상시 로드를 넣었다. 경로는 설치 위치에 종속되지 않도록 `${CLAUDE_PLUGIN_ROOT}`를 쓴다. 필수 파일을 읽지 못하면 완료 판정을 금지하고 검증 공백으로 보고한다.

**외부 기준 확인 주체 확정**: 실행 주체가 없는 "월 1회 확인"과 90일 예외 규칙을 폐기했다. OWASP·web.dev 기준이 든 참고자료를 읽는 실행 주체가 `확인일`을 보고, 작업일 기준 30일을 넘었을 때만 변경 신호(OWASP 최신 commit SHA, web.dev `Last updated`와 임계값 영역)를 확인한다. 변경이 없으면 기록값을 쓰고 결과를 보고하며, 변경이 있으면 자동으로 수치를 고치지 않고 `needs_review`를 보고한다. 스킬 저장소 유지보수 작업일 때만 두 사본과 변경 이력을 함께 갱신한다. 공식 출처를 확인할 수 없으면 새 운영 기준을 현행 공식값으로 단정하지 않는다.

**Core Web Vitals 구간 경계**: `개선 필요` 열이 `좋음` 구간을 포함하던 표기를 열린 하한·닫힌 상한으로 바꿨다. LCP는 `> 2.5s, ≤ 4.0s`, INP는 `> 200ms, ≤ 500ms`, CLS는 `> 0.1, ≤ 0.25`다. 공식 임계값 자체는 바꾸지 않았다.

## 성공 기준 식별자를 §글자-번호로 전환 + 테스트 이름 서술형 (be-workflow 0.1.3 / be-review 0.1.4)

**계기**: 성공 기준을 전역 순번 SC-1, SC-2…로 매기던 방식은 스펙을 합본할 때 번호가 밀린다. fanclub-service-api의 additional-info 통합 스펙에서 실제로 발생했다 — 합본 전 로컬 SC 번호를 든 검증 리포트·ADR이 현행 전역 번호와 불일치했고, `@DisplayName("SC-N: …")` 태그도 함께 어긋났다. 번호를 리매핑하는 규칙(0.1.x의 "스펙 통합·재번호 시 태그도 리매핑")은 사고를 막지 못했다.

**변경 1 — 식별자**: 전역 SC-N을 폐기하고 `§글자-번호`(예: `§G-1`)로 바꿨다. §글자는 성공 기준 절 아래의 기능 묶음(`### §A.`)이고 한 번 부여하면 바꾸지 않으며 재사용하지 않는다. 묶음 안 번호는 추가 전용이다(삭제는 줄을 남기고 `(삭제 …)` 표시). 합본이 예정된 증분 스펙은 처음부터 통합 스펙의 미사용 글자를 쓰므로 합본 때 글자·번호가 그대로 옮겨진다. 다른 스펙의 기준은 `<spec_id>#§A-1`로 가리킨다 (기존 `<spec_id>#SC-N` 대체). 대안이었던 서술형 슬러그(`§J:조회-새경로`)는 상호 참조가 길어지고 슬러그 중복 관리가 필요해 채택하지 않았다.

**변경 2 — 테스트 이름**: `@DisplayName`에서 기준 식별자를 뺀다. 그 자리에 무엇을 검증하는지 서술한다 — 대상 기능·조건·기대 결과가 이름만으로 드러나야 한다(예: "저장된 생년월일과 다른 birthYear로 저장하면 400/-3으로 거절한다"). 번호만 지우고 이름이 모호해지면 안 되므로 두 변경은 짝이다. 기준 ↔ 테스트 연결은 계획(PLAN.md)의 추적성 표 한 곳에서만 관리한다 — 사람과 에이전트가 함께 보는 단일 색인이며, "모든 기준이 덮였는가", "이 기준을 검증하는 테스트가 무엇인가"의 판정 근거다. 표가 낡으면 추적성 결함이다.

**연쇄 수정**: spec-template(5절 구조·식별자 규칙·상호 참조·체크리스트), spec-writer(예시·승인 후 수정 시 번호 밀기 금지·체크리스트), task-breakdown·plan-template(추적성 표가 유일한 연결 장소, 테스트 이름 열이 곧 `@DisplayName`), plan-executor(서브에이전트 규칙에 이름 규칙 추가, 대조 후 계획 표를 실제 이름으로 갱신, 보고에 "추적성 표 갱신" 항목), spec-conformance-check(4단계를 SC 태그 확인에서 매핑 표 기준 확인으로 재작성 — 오케스트레이터 수행, 불명확한 이름·낡은 표를 추적성 결함으로 보고, 보고서에 "추적성" 절), tdd(이름 원칙 재작성), code-review-and-quality(테스트 리뷰에서 "이름이 무엇을 검증하는지 서술하는가" 강조). 개별 저장소의 스펙·테스트·리포트 반영은 별도 작업이다.
