# 커스텀 내역

이 스킬 세트는 [addyosmani/agent-skills](https://github.com/addyosmani/agent-skills)와 기존에 쓰던 plan-writer/plan-executor/adr-writer를 조합해 만들었다. 그대로 쓰지 않고 커스텀한 이유는 하나로 요약된다: **addyosmani 스킬들은 "한 에이전트가 순서대로 일하는" 전제로 쓰였고, 우리 워크플로우는 "사람 승인 게이트 + 고비용 계획/저비용 실행 분리 + 문서로 상태 관리"가 전제라서, 그 사이의 빈틈을 메워야 했다.**

각 항목은 "원본은 어땠나 → 뭐가 문제였나 → 그래서 어떻게 바꿨나" 순서로 기록한다.

## spec-writer — "스펙 쓰고 멈추는" 스킬

**원본**: addyosmani `spec-driven-development`. 스펙 작성 → 계획 → 작업 분해 → 구현까지 4단계를 한 스킬이 전부 진행한다.

**문제**: 우리 워크플로우에서 계획과 실행은 별도 스킬(task-breakdown, plan-executor)의 일이다. 원본을 그대로 쓰면 역할이 겹치고, 더 큰 문제는 에이전트가 스펙 승인을 기다리지 않고 계획까지 이어서 달린다는 것. 스펙이 반려되면 그 계획 작업은 전부 버려진다.

**변경**: 스펙 작성(Phase 1)만 남기고 뒷단계를 전부 잘라냈다. "docs/spec/에 저장하고 승인을 요청한 뒤 **종료한다**"를 명시. 여기에 세 가지를 새로 넣었다:

- **성공 기준마다 SC-1, SC-2… ID를 붙인다.** 뒤에서 계획이 이 기준을 빠짐없이 커버하는지 대조하고(task-breakdown), 구현이 이 기준을 실제로 충족하는지 검증할 때(spec-conformance-check) 항목 단위로 참조하기 위해서다. ID가 없으면 "대충 다 반영했겠지"가 된다.
- **문서 머리에 YAML frontmatter(`status: draft/approved`)를 붙인다.** 사람이 아니라 다음 스킬이 "이 스펙 승인됐나?"를 기계적으로 판단하는 용도.
- **확인 못 한 가정이 3개 쌓이면 스펙 쓰기를 멈추고 interview-me(인터뷰 스킬)로 전환한다.** 가정 위에 쓴 스펙은 스펙이 아니라 소설이기 때문.

## task-breakdown — 방법론은 addyosmani, 문서 포맷은 기존 체계

**원본 두 개를 합쳤다**: 기존 plan-writer(계획 문서 작성 스킬) + addyosmani `planning-and-task-breakdown`(작업 분해 방법론).

**문제 1 — plan-writer가 너무 많은 일을 했다.** 요구사항 인터뷰부터 계획 작성까지 한 스킬이었는데, 인터뷰는 이제 interview-me/spec-writer의 일이다. 그래서 인터뷰 절차를 들어내고, 대신 "승인된 스펙이 없으면 시작하지 않는다"는 입력 조건을 달았다.

**문제 2 — addyosmani의 계획 문서 포맷은 우리 실행기와 호환이 안 된다.** plan-executor는 계획 문서의 YAML frontmatter를 파싱해서 움직인다: `status: approved`인지 확인하고(승인 게이트), `depends_on`으로 병렬 실행 순서를 정하고, `tier`(standard/light)로 작업마다 고비용/저비용 모델을 배정한다. addyosmani 포맷은 체크박스 마크다운이라 이 자동화가 전부 죽는다. 그래서 **분해하는 요령(수직 슬라이싱, 작업 크기 제한, 고위험 먼저, 체크포인트)은 addyosmani에서 가져오고, 문서 포맷은 plan-writer 템플릿을 유지**했다.

**신설 — 추적성 매핑.** 스펙의 모든 SC-N이 계획의 어느 작업에서 충족되는지 표로 만들어 계획 문서에 넣는 것을 의무화했다. 커버 안 된 SC가 있으면 계획 반려. "계획이 스펙의 일부를 빼먹는" 사고를 계획 단계에서 잡는 예방 장치이며, 두 원본 어디에도 없던 규칙이다.

## spec-conformance-check — 완전 신규

**왜 만들었나**: 기존 검증에 구멍이 있었다. plan-executor는 "계획대로 됐는가"를 검증하고, 리뷰 스킬들은 "코드가 잘 짜였는가"를 본다. 그런데 **계획 자체가 스펙을 잘못 옮겼다면?** 계획 기준 검증은 전부 통과하고도 스펙과 다른 물건이 나온다. 이 "맞는 것을 만들었는가" 검증이 어디에도 없었다.

**핵심 설계 — 검증자 격리**: 검증은 새 서브에이전트가 하는데, **스펙 문서와 코드만 주고 계획 문서는 주지 않는다.** 계획을 보여주면 계획의 잘못된 해석까지 물려받아서 독립 검증이 안 되기 때문.

그 외 규칙:

- 판정에는 반드시 실행 증거(테스트·API 호출 결과)가 있어야 한다. "코드 읽어보니 있는 것 같다"는 금지.
- 하나라도 FAIL이면 품질 리뷰로 넘어가지 않고 계획 수정으로 되돌린다. 틀린 기능의 품질 리뷰는 낭비다.
- 검증자가 발견한 김에 고치는 것도 금지. 수정이 승인 게이트를 우회하게 되기 때문이다.

fresh-context 교차 검증 아이디어는 addyosmani `doubt-driven-development`에서 차용했다.

## plan-executor — 기존 스킬에 두 가지 연결 추가

**변경 1**: 완료 보고에 "다음은 spec-conformance-check"를 명시하게 했다. "전체 검증 통과 = 끝"이라는 착각을 막기 위해, 타협 금지 표에도 "전체 검증 통과했으니 스펙도 맞을 것"이라는 변명을 추가했다.

**변경 2**: ADR 관련 역할을 바꿨다 (아래 adr-writer 참고). 실행 전에 계획에 연결된 Proposed ADR이 있는지 확인하고, 실행 중 결정이 바뀌면 supersede를 제안하며, Accepted 확정은 스펙 적합성 검증 통과 후로 미룬다.

## adr-writer — 기록 시점을 결정 시점으로 당김

**원본 방식**: 구현이 다 끝난 뒤 ADR(아키텍처 결정 기록)을 쓴다.

**문제**: DB 선택 같은 되돌리기 비싼 결정은 실제로는 **계획 승인 시점**에 내려진다. 기록이 실행 완료 후면, (a) 실행이 중간에 엎어지면 결정 이력이 사라지고, (b) 결정이 "대안은 뭐였고 왜 기각했나" 포맷으로 검토받지 못한 채 승인된다.

**변경**: 2시점으로 분리했다.

- 계획 승인 직후 `Proposed`로 초안 작성 — 계획서의 판단 근거 섹션이 재료라 10분 일이다.
- 스펙 적합성 검증 통과 후 `Accepted`로 확정. 실행 중 결정이 바뀌면 기존 것을 고치지 않고 새 ADR로 대체(supersede)한다.

## interview-me — 원본 그대로, 이름표만 수정

방법론은 손대지 않았다. 문서 끝의 "다른 스킬과의 연계(Interaction with Other Skills)" 섹션이 addyosmani 스킬명(spec-driven-development 등)을 가리키고 있어서, 우리 스킬명(spec-writer 등)으로만 바꿨다. 안 바꾸면 에이전트가 존재하지 않는 스킬을 호출하려 든다.

## 리뷰 3종 + 페르소나 2종 — 번역만

내용 무변경 한글 번역 (code-review-and-quality, security-and-hardening, performance-optimization + code-reviewer, security-auditor).

단 원본 리뷰 세트에 있던 `code-simplification`은 **일부러 뺐다** — 이건 검토 스킬이 아니라 코드를 고치는 스킬이라, 병렬로 돌리면 다른 리뷰어가 보고 있는 코드가 중간에 바뀐다. 코드 수정은 어떤 경우든 task-breakdown 루프(계획→승인→실행)를 통해서만 한다.
